---
- name: Base server bootstrap
  hosts: servers
  become: true
  gather_facts: true

  roles:
    - role: common
      tags: common

    - role: users
      tags: users

    - role: ssh_hardening
      tags: ssh

    - role: firewall
      tags: firewall

    - role: observability
      tags: observability

    - role: secrets
      tags: secrets

    - role: healthcheck
      tags: healthcheck